# Universal data prepare API

## Features

## Usage
```python
from gxdata import Jsoner

jsoner = Jsoner("hello.json")

jsoner.keys()  # get keys of dict type
jsoner.Type()  # get type of json
jsoner.sample()  # sample of whole json, default 5 item
```